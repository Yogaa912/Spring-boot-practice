package com.example;

public class UserRepository {
    public String getUserById(int id) {
        // 真实情况下，这里可能是数据库查询
        return "User-" + id;
    }
    public String getUserName(int id) {
        // 真实情况下，这里可能是数据库查询
        return "User-" + id;
    }
}
